[project]
name = "kitsune"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "logfire",
    "nicegui",
    "pydantic-ai>=1.58.0",
    "uvicorn[standard]",
]
